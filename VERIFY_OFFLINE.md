# Verify offline behavior

1. Run `build-offline.bat`.
2. Disconnect the network or enable browser DevTools Offline mode.
3. Open `dist/index.html`.
4. Load a PDF, reorder/rotate/delete pages, undo, preview, and export.
5. Confirm there are no runtime network requests.
